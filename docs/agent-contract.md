# Agent Contract

> Rolling log of acknowledgements made by AI agents working on this codebase.
> Seed prompts that require an agent to "confirm" or "acknowledge" something write here.
>
> **Format:** one entry per acknowledgement, newest at the top. Each entry lists: date (ISO), prompt id, agent identity, what was acknowledged.

---

## 2026-04-18 · [IV.19.1] · Claude Opus 4.7 (1M context)

**Acknowledgement:** System Rules installed to `CLAUDE.md` at repo root. Agent confirms:

- Every session will read `CLAUDE.md` before acting.
- Hard constraints 1–13 are binding.
- Output-format block will close every session.
- Self-check list will run before declaring any prompt DONE.
- Execution workflow (one-prompt-per-session, commit-per-prompt, ask-to-proceed) is in force for this project.

---

*(Future acknowledgements append above this line.)*
