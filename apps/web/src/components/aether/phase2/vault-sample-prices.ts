/**
 * Vault sample prices — re-export from `@app/aether-canvas-shared`.
 *
 * Implementation moved to the shared package in AE479 so the Phase 4
 * native Vault demo uses identical fixture data. This file remains so
 * existing imports keep working.
 */
export {
  SAMPLE_VAULT_MAX_AMOUNT,
  SAMPLE_VAULT_MIN_AMOUNT,
  SAMPLE_VAULT_PRICES,
} from '@app/aether-canvas-shared';
