{
  "name": "@app/aether-canvas-shared",
  "version": "0.0.0",
  "private": true,
  "description": "Aether 2.0 shared canvas helpers — pure math (lifecycle progress, camera poses, spatial layouts, breathing envelopes, weather + time, destination coords). Framework-free so both @app/aether-canvas (web R3F) and the Phase 4 @app/aether-canvas/native (RN R3F) consume identical contracts.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint src test",
    "test": "jest --passWithNoTests",
    "test:watch": "jest --watch"
  },
  "dependencies": {
    "@app/aether-core": "workspace:*"
  },
  "devDependencies": {
    "@app/eslint-config": "workspace:*",
    "@app/tsconfig": "workspace:*",
    "@types/jest": "^29.5.14",
    "@types/node": "^22.10.5",
    "jest": "^29.7.0",
    "ts-jest": "^29.4.12",
    "typescript": "^5.7.2"
  }
}
