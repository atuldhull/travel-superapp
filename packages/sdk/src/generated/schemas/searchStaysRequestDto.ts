// @ts-nocheck
// AUTO-GENERATED by orval. Do not edit.
// Regenerate via: pnpm --filter=@app/sdk sdk:gen
import type { StaysCoordinatesDto } from './staysCoordinatesDto';

export interface SearchStaysRequestDto {
  center: StaysCoordinatesDto;
  radiusKm: number;
  /** ISO date YYYY-MM-DD. */
  checkIn: string;
  /** ISO date YYYY-MM-DD; must be after checkIn. */
  checkOut: string;
  /** Guest count (default 2). */
  guests?: number;
  /**
   * V.UX.14 — required amenities (case-insensitive substring match). Family-mode UI sends ["crib","high_chair","stroller_accessible"].
   * @maxItems 10
   */
  requiredAmenities?: string[];
  /** V.UX.16 — exact match against StayListing.stayType (e.g. "hostel", "boutique"). */
  stayType?: string;
  /** V.UX.16 — listings whose priceUsdPerNight exceeds this cap are dropped. Listings without a quoted price pass through. */
  maxPriceUsdPerNight?: number;
  /** V.UX.23 — drop listings whose wifiSpeedMbps falls below this floor (or whose wifi speed is unknown). Drives the nomad-mode wifi-speed slider. */
  minWifiSpeedMbps?: number;
}
