// @ts-nocheck
// AUTO-GENERATED by orval. Do not edit.
// Regenerate via: pnpm --filter=@app/sdk sdk:gen
import type { TripDtoStartsOn } from './tripDtoStartsOn';
import type { TripDtoEndsOn } from './tripDtoEndsOn';

export interface TripDto {
  id: string;
  /** Owner's user id. */
  userId: string;
  /** Owner-chosen title (1..120 chars). */
  title: string;
  /** Lifecycle status — currently always "draft" until itinerary AI lands. */
  status: string;
  /**
   * Trip planning radius in km (1..500).
   * @minimum 1
   * @maximum 500
   */
  radiusKm: number;
  /**
   * ISO-8601; null until the owner picks a date range.
   * @nullable
   */
  startsOn: TripDtoStartsOn;
  /**
   * ISO-8601; null until the owner picks a date range. >= startsOn when both set.
   * @nullable
   */
  endsOn: TripDtoEndsOn;
  /** Bumped on every accepted update — concurrency control. */
  version: number;
  createdAt: string;
  updatedAt: string;
}
