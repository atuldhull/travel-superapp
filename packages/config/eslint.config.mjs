/**
 * Flat ESLint config for @app/config. Re-exports the shared monorepo preset.
 * Override rules here only if this package needs special treatment.
 */
import config from '@app/eslint-config';

export default config;
